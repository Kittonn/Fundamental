#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define a 2

int main() {
    
    /*
        int arr[] = {1,2,3,4};
        arr[1] = 6;
        printf("%d",arr[1]);
    */
   
    const int e = 2;
    int arr[a];
    arr[0] = 1;
    arr[1] = 200;

    printf("%d %d",arr[0],arr[1]);
    
    return 0;
}